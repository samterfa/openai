% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/testing.R
\name{create_chat_completions}
\alias{create_chat_completions}
\title{Generate a conversation with an AI model}
\usage{
create_chat_completions(
  model,
  messages,
  max_tokens = NULL,
  temperature = NULL,
  top_p = NULL
)
}
\arguments{
\item{model}{A string representing the model to be used for the chat completion.}

\item{messages}{A list of message objects. Each object should have a 'role' which can be 'system', 'user', or 'assistant', and 'content' which is the text of the message from the role.}

\item{max_tokens}{An optional integer representing the maximum length of the response. If not provided, the model will decide.}

\item{temperature}{An optional number representing the creativity of the output. Higher values (close to 1) make the output more creative, while lower values make it more focused and deterministic.}

\item{top_p}{An optional number representing the nucleus sampling parameter. It should be between 0 and 1.}
}
\value{
A list where the 'choices' field contains the generated message(s) from the assistant. Each message is a list with 'role' and 'content' fields, where 'role' is 'assistant' and 'content' is the text of the message.
}
\description{
This function allows you to have a conversation with an AI model. You provide a series of messages and the model generates a response.
}
\examples{
\dontrun{
create_chat_completions("gpt-3.5-turbo", list(list("role" = "system", "content" = "You are a helpful assistant."), list("role" = "user", "content" = "Who won the world series in 2020?")))
}
}
\references{
OpenAI API documentation: \url{https://platform.openai.com/docs/api-reference/chat/create}
OpenAI API models: \url{https://platform.openai.com/docs/guides/chat/models}
OpenAI API chat guide: \url{https://platform.openai.com/docs/guides/chat}
}
